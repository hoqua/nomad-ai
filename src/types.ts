export type DestinationItem  =  { location: string, description: string, img: string }

export enum SearchKeys {
    Location = 'location',
    Month = 'month',
    Budget = 'budget',
    Activity = 'activity',
}